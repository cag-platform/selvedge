node_modules/
dist/
dist-server/
# All dotenv files (incl. Vite's .env.production / .env.development / .env.*.local),
# but keep the committed template.
.env
.env.*
!.env.example
*.log
coverage/
.build/
.DS_Store

# Throwaway visual-audit harness (fixtures + screenshots). Never shipped.
.audit/

# QA fixtures emitted by `npm run fixtures` (scripts/make-fixtures.ts). The
# generator is versioned; its output is scratch.
fixtures/
